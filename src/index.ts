import { Start } from './Start';
import ThemeConfig from './ThemeConfig';
import { ThemeContext, ThemeProvider } from './provider';
import useTheme from './useTheme';
import useThemeObject from './useThemeObject';
const SwksThemeReact = {
  Start,
  ThemeProvider,
  ThemeContext,
  useTheme,
  useThemeObject,
  ThemeConfig,
};
export default SwksThemeReact;

export {
  Start,
  ThemeProvider,
  ThemeContext,
  useTheme,
  useThemeObject,
  ThemeConfig,
};
